export const BCRYPT_SALT_ROUNDS = 10;
export const JWT_DEFAULT_SECRET_KEY = "jwt-secret-key";

export const AUTH_ROUTE_NAMESPACE = "/auth";
export enum AuthRoutePaths {
  LOGIN = "/login",
  LOGOUT = "/logout",
}
